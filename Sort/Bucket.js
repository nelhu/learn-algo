const bucketSort = array => {
  // 申请桶bucket
  // 遍历单个元素，放入bucket
  // 排序单个bucket
  // 依次取出bucket中排好序的元素
}

const testArr = [ 3, 9, 1, 96, 34, 56, 67, 23, 12, 47, 87, 92, 26, 38, 73, 49, 51, 66, 98 ];